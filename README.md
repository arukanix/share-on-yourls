# Share on YOURLS

WordPressの投稿ページから、自作の短縮URLサーバー「YOURLS」のAPIを利用して自動で短縮URLを生成・共有できるようにするプラグインです。

## 📥 ダウンロード
手軽にインストールしたい場合は、以下のリンクから最新のZIPファイルをダウンロードしてください。

👉 **[Share on YOURLS をZIPでダウンロードする (mainブランチ版)](./share-on-yourls.zip)**

---

## ✨ 主な機能

- **短縮URLの自動生成**: 投稿が表示されたタイミングで、YOURLS API経由で自動的に短縮URLを発行します。
- **データベースキャッシュ**: 一度生成した短縮URLはカスタムフィールド（メタデータ）に保存されるため、2回目以降のアクセスで無駄なAPI通信（負荷）が発生しません。
- **管理画面での一覧表示**: 投稿一覧画面に「短縮URL」カラムが追加され、生成済みのURLをひと目で確認・アクセスできます。
- **安全な設定画面**: 管理画面からいつでもAPIキーとYOURLSのサイトURLを変更・保存できます（管理者権限のみアクセス可能）。

## 💻 動作環境

- WordPress 6.0 以上（推奨）
- PHP 7.4 以上
- 事事前に構築済みの **YOURLS** サーバーと、有効な **APIキー（Signature）**

## 🚀 使い方・インストール方法

### 1. プラグインのインストール
1. 上記の [ZIPでダウンロードする](./share-on-yourls.zip) リンクからファイルをダウンロードします。
2. WordPress管理画面の **プラグイン ＞ 新規追加 ＞ プラグインのアップロード** へ移動します。
3. ダウンロードした `share-on-yourls.zip` を選択してインストールします。
4. インストール完了後、**Share on YOURLS** を有効化します。

### 2. 初期設定
1. WordPress管理画面の **設定 ＞ YOURLS APIキー設定** に移動します。
2. お使いのYOURLSサーバーから取得した **APIキー** と、**サイトURL**（例: `https://your-domain.com`）を入力します。
3. 「設定を保存」ボタンをクリックします。

### 3. 表示確認
1. 設定完了後、公開されているいずれかの「投稿（記事）」ページを一度ブラウザで閲覧します。
2. これによりバックグラウンドで短縮URLが自動生成されます。
3. WordPress管理画面の「投稿一覧」を確認すると、生成された短縮URLが追加されていることが確認できます。

## 📁 フォルダ構成

本プラグインを動作させるには、以下のファイル構造である必要があります。

```text
share-on-yourls/
├── share-on-yourls.php  # プラグインのメインコード
├── template.html        # ポップアップ/表示用のHTMLテンプレート
├── share-on-yourls.zip  # インストール用ZIPファイル
└── README.md            # このファイル
```

## 🔒 ライセンス
MIT License
